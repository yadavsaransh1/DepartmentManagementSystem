spring.application.name=Department Management System

server.port=${PORT:8080}
server.address=0.0.0.0

# CORS Configuration - Centralized Origins
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:5178,http://localhost:5179,http://localhost:5180,http://localhost:5181,http://localhost:5182,http://localhost:5183,http://localhost:5184,http://localhost:5185,http://localhost:5186,http://localhost:5187,http://localhost:5188,http://localhost:5189,http://localhost:5190,https://department-management-system-438o.vercel.app

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/university_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:yadavji}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true

# Disable Quartz scheduler to avoid database lock issues
spring.quartz.enabled=false

# CORS is now configured in SecurityConfig using app.cors.allowed-origins property above
# No need for spring.mvc.cors settings as they are handled by SecurityFilterChain

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

jwt.secret=your-secret-key-change-this-in-production-environment-very-long-and-secure-key
jwt.expiration=86400000

app.frontend.url=http://localhost:5174

# ========== EMAIL CONFIGURATION (Password Reset & Notifications) ==========
# Gmail SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=saranshyadav85208@gmail.com
spring.mail.password=zqgc kekz jkfb gvyv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Sender email address
spring.mail.properties.mail.from=saranshyadav85208@gmail.com

# ========================================================================

logging.level.root=WARN
logging.level.com.university=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN
